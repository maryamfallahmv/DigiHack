import type {Person} from './person';

/**
 * Oppgave 39 — Tell pensjonister (10 poeng)
 *
 * Tell hvor mange personer i listen som er pensjonister. En person er
 * pensjonist når alderen er 67 eller mer (alder >= 67).
 *
 *     [lagPerson("Aisha",30), lagPerson("Hassan",80), lagPerson("Astrid",67)]  →  2
 *     [lagPerson("Aisha",30), lagPerson("Lin",45)]                             →  0
 *     []                                                                       →  0
 */
export function antallPensjonister(personer: Person[]): number {
  // TODO
  throw new Error('Oppgave 39 — ikke implementert ennå');
}
