/**
 * Oppgave 31 — Tell per kommune (15 poeng)
 *
 * Tell hvor mange ganger hver kommune forekommer i listen, og returnér
 * resultatet som et oppslag fra kommunenavn til antall.
 *
 *     ["Oslo", "Oslo", "Bergen"]  →  Map { Oslo => 2, Bergen => 1 }
 *     ["Oslo"]                     →  Map { Oslo => 1 }
 *     []                           →  Map {}
 */
export function tellPerKommune(kommuner: string[]): Map<string, number> {
  // TODO
  throw new Error('Oppgave 31 — ikke implementert ennå');
}
