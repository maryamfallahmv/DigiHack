/**
 * Oppgave 32 — Poststed-oppslag (15 poeng)
 *
 * Slå opp et poststed ut fra postnummeret i en oppslagstabell. Finnes
 * ikke postnummeret i tabellen, returnér "Ukjent".
 *
 *     tabell = Map { 0150 => Oslo, 5003 => Bergen }
 *     poststed("0150", tabell)  →  "Oslo"
 *     poststed("9999", tabell)  →  "Ukjent"
 */
export function poststed(postnummer: string, tabell: Map<string, string>): string {
  // TODO
  throw new Error('Oppgave 32 — ikke implementert ennå');
}
