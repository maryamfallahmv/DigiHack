/**
 * Oppgave 15 — Initialer (10 poeng)
 *
 * Lag initialene til et navn: del navnet på mellomrom, ta første bokstav
 * i hvert ord, gjør den stor, og slå dem sammen.
 *
 *     "Aisha Khan"       →  "AK"
 *     "Per Olav Hansen"  →  "POH"
 *     "Aisha"            →  "A"
 *     ""                 →  ""
 *
 * Ignorer tomme ledd (f.eks. ekstra mellomrom).
 */
export function initialer(navn: string): string {
  // TODO
  throw new Error('Oppgave 15 — ikke implementert ennå');
}
