/**
 * Oppgave 35 — Mest vanlige kommune (20 poeng)
 *
 * Returnér kommunen som forekommer flest ganger i listen. Ved likhet
 * vinner den kommunen som NÅR det høyeste antallet FØRST når du går
 * gjennom listen. En tom liste gir "".
 *
 *     ["Oslo", "Bergen", "Oslo"]  →  "Oslo"
 *     ["Oslo", "Bergen"]          →  "Oslo"
 *     []                          →  ""
 */
export function mestVanlige(kommuner: string[]): string {
  // TODO
  throw new Error('Oppgave 35 — ikke implementert ennå');
}
