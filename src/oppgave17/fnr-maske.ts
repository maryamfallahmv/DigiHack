/**
 * Oppgave 17 — Maskér fødselsnummer (15 poeng)
 *
 * For å vise et fødselsnummer trygt i en logg beholder vi de 4 siste
 * tegnene og erstatter alle de foran med "*":
 *
 *     "01059012345"  →  "*******2345"
 *     "98765432109"  →  "*******2109"
 *
 * Dette er IKKE validering — du skal ikke sjekke Mod-11 eller noe annet,
 * bare maskere tegnene.
 */
export function maskerFnr(fnr: string): string {
  // TODO
  throw new Error('Oppgave 17 — ikke implementert ennå');
}
