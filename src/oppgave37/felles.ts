/**
 * Oppgave 37 — Felles søkere (15 poeng)
 *
 * Finn snittet (de elementene som finnes i BEGGE listene). Resultatet
 * skal være i samme rekkefølge som de dukker opp i den første listen
 * (a), og uten duplikater.
 *
 *     felles(["1","2","3"], ["2","3","4"])  →  ["2","3"]
 *     felles(["1","2"],     ["3","4"])      →  []
 *     felles(["1","1","2"], ["1","2"])      →  ["1","2"]
 */
export function felles(a: string[], b: string[]): string[] {
  // TODO
  throw new Error('Oppgave 37 — ikke implementert ennå');
}
