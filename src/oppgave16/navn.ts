/**
 * Oppgave 16 — Stor forbokstav (15 poeng)
 *
 * Gjør første bokstav i hvert ord stor og resten små. Behold enkle
 * mellomrom mellom ordene.
 *
 *     "aisha khan"       →  "Aisha Khan"
 *     "AISHA KHAN"       →  "Aisha Khan"
 *     "per olav hansen"  →  "Per Olav Hansen"
 *     ""                 →  ""
 */
export function kapitaliser(navn: string): string {
  // TODO
  throw new Error('Oppgave 16 — ikke implementert ennå');
}
