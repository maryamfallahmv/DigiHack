/**
 * Oppgave 38 — Mangler i listen (15 poeng)
 *
 * Finn de elementene i FORVENTET som IKKE finnes i FAKTISK. Resultatet
 * skal være i samme rekkefølge som i forventet, og uten duplikater.
 *
 *     mangler(["1","2","3"], ["2"])      →  ["1","3"]
 *     mangler(["1","2"],     ["1","2"])  →  []
 *     mangler(["1","2","3"], [])         →  ["1","2","3"]
 */
export function mangler(forventet: string[], faktisk: string[]): string[] {
  // TODO
  throw new Error('Oppgave 38 — ikke implementert ennå');
}
