/**
 * Oppgave 23 — Første over grense (15 poeng)
 *
 * Finn INDEKSEN til det første beløpet i lista som er strengt større
 * enn grensen. Returner -1 hvis ingen er over grensen.
 *
 *     forsteOver([10, 20, 30], 15)   →  1   (20 er først over 15)
 *     forsteOver([10, 20, 30], 100)  →  -1  (ingen er over 100)
 *     forsteOver([], 5)              →  -1
 */
export function forsteOver(inntekter: number[], grense: number): number {
  // TODO
  throw new Error('Oppgave 23 — ikke implementert ennå');
}
