import type {Person} from './person';

/**
 * Oppgave 40 — Finn eldste søker (15 poeng)
 *
 * Finn personen med høyest alder. Hvis flere har samme høyeste alder,
 * skal den FØRSTE i listen velges. Hvis listen er tom, finnes det ingen
 * eldste — returnér undefined.
 *
 *     [lagPerson("Hassan",80), lagPerson("Lin",45)]       →  Hassan
 *     [lagPerson("Astrid",70), lagPerson("Hassan",70)]    →  Astrid (først)
 *     []                                                  →  undefined
 */
export function finnEldste(personer: Person[]): Person | undefined {
  // TODO
  throw new Error('Oppgave 40 — ikke implementert ennå');
}
